	using 只是指出了该名称什么时候可以使用和作用域，变量的真正定义位置是在namespace中代码第一次被读到的时候。
	所以名称空间中的定义和名称除了使用的时候需要加名称空间的前缀::以外，没有任何特殊之处，在include或者在使用之前在.cpp读到的时候生成定义，与静态外部变量或静态内部变量类似，只是他们是::，外域的。
	想不通的时候就想他们没有命名空间，只是普通::外域的声明和定义，也可以在不同的文件中（分为.h和.cpp).